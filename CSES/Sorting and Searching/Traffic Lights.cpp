#include <bits/stdc++.h>

using namespace std;

struct dist {
    int i;
    int f;
    int dist;
};

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int x,n; cin >> x >> n;

    set<int> s;
    set<dist> s;
    s.insert(0),s.insert(x);


    int k,res = 0;
    while(n--) {
        cin >> k;
        

    }
    cout << "\n";

    return 0;
}