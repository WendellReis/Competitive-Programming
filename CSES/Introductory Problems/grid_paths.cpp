#include <bits/stdc++.h>

using namespace std;

const int N = 7;
int tot = 0;

void searchPaths(bool grid [][N], int x, int y, string s, int tam);

int main(){
    bool grid[N][N];
    for(int i = 0; i < N; i++)
        for(int j = 0; j < N; j++)
            grid[i][j] = false;

    string path; cin >> path;

    return 0;
}

void searchPaths(bool grid [][N], int x, int y, string s, int tam){
    
}