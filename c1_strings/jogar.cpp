#include <bits/stdc++.h>

using namespace std;

int main() {
    int N;
    cin >> N;

    string s;
    getline(cin >> ws, s);
    for(int i = 1; i <= N; i++)
        cout << s << "\n";

    return 0;
}