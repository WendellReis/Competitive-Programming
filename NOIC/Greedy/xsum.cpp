#include <bits/stdc++.h>

using namespace std;

int main(){
    int t; cin >> t;

    while(t > 0){
        int n,m; cin >> n >> m;

        int mat[n][m];
        for(int i = 0; i < n; i++)
            for(int j = 0; j < m; j++)
                cin >> mat[i][j];

        long long di0[n] = {0}, din[m] = {0};
        long long dj0[n] = {0}, djn[m] = {0};
        t--;
    }
    return 0;
}