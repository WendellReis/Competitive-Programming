#include <bits/stdc++.h>

using namespace std;

#define ll long long

int main(){
    int t; cin >> t;

    while(t > 0){
        int n,m; cin >> n >> m;

        ll board[n][m];
        for(int i = 0; i < n; i++)
            for(int j = 0; j < m; j++)
                cin >> board[i][j];
        
        
        t--;
    }
    return 0;
}