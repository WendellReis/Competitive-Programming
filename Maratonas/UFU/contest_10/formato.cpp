#include <bits/stdc++.h>

using namespace std;

int main(){
    int t; cin >> t;
    while(t > 0){
        string user, prog;
        getline(cin,user);
        getline(cin,prog);

        bool tam = prog.size() > user.size();
        t--;
    }
    return 0;
}